import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  question: {
    color: "#fafafa",
    fontSize: 66,
    position: 'absolute',
    bottom: 0,
    right: 0,
    padding: 10
  }
})

export default styles
